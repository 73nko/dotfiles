# Raycast — Glacier Signal

Raycast theme activation is a manual import. The repository checker validates
the tracked JSON colors but cannot inspect Raycast's selected live theme.

## Import

1. Open Raycast.
2. Run **Import Theme**.
3. Select `~/.config/raycast/glacier-signal.json`.
4. In **Settings → Appearance → Theme**, confirm **Glacier Signal** is selected.

## Semantic slots

- `background`: night `#062230`
- `backgroundSecondary`: fjord `#0d3547`
- `text`: snow `#f3faf7`
- `selection` and `purple`: signal `#22b8f5`
- `loader`: ice `#7fe0ff`
- `red`: coral `#ff7a6e`
- `orange` and `yellow`: gold `#ffd873`
- `green`: green `#8fdc8f`
- `blue`: azure `#b8f1ff`
- `magenta`: teal `#5ff2cf`
